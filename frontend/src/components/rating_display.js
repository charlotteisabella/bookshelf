import React from "react";
import styles from "./rating_display.css"
import CSSModules from "react-css-modules"


const RatingDisplay = () => {
  
};

export default CSSModules(RatingDisplay, styles);
